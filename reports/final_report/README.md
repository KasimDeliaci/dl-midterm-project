# Report Build Notes

The report uses a self-contained two-column LaTeX article template with standard packages.
This avoids depending on external course style files such as:

- `comp541.sty`
- `comp541.bst`

Build `main.tex` from this directory with your preferred LaTeX toolchain. The generated PDF and temporary build files should stay out of Git.
