# Tech Debt

Track deferred cleanup here once implementation begins.

## Open

- Implement Sprint 3 fusion modules and replace the `scripts/run_experiment_matrix.py` placeholder with real matrix orchestration.
- Replace remaining placeholder tests with real coverage where scope exists, especially `tests/test_config_loading.py` and `tests/test_fusion_shapes.py`.
- Add tests for config loading and path resolution.
- Add report build command once local LaTeX tooling is selected.
